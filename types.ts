import { IconType } from "react-icons";

export interface IService{
  title: string,
  details: string,
  Icon: IconType
}

export interface ISkill{
  name: string,
  level: string,
  Icon: IconType
}